import type { Framework } from "../types";

export const celeborn: Framework = {
  id: "celeborn",
  name: "Apache Celeborn",
  short: "Celeborn",
  category: "Compute",
  color: "#FF6B6B",
  tagline: "Elastic remote shuffle service for Spark and Flink",
  signals: [
    {
      id: 1,
      tag: "Architecture",
      headline: "Celeborn decouples shuffle from compute — executors become truly stateless",
      detail: "Celeborn runs as a dedicated Remote Shuffle Service: 3+ Masters (Raft consensus for HA) manage metadata and slot assignment; Workers (one pod per node) store shuffle data. Executors write shuffle to Celeborn Workers and exit cleanly. Karpenter can consolidate compute nodes mid-job without recomputing shuffle because no shuffle data lives on executor nodes.",
      metric: "Stateless",
      metricLabel: "executors with external shuffle",
    },
    {
      id: 2,
      tag: "Dynamic Allocation",
      headline: "Celeborn is the missing piece that makes Spark DRA production-safe",
      detail: "Spark Dynamic Resource Allocation without an external shuffle service loses shuffle data when executors scale down — requiring expensive recomputation. With Celeborn, DRA scales executors to zero between stages and back up instantly. Shuffle data persists on Celeborn workers regardless of executor count. TPC-DS 10TB validated: zero job failures with DRA enabled.",
      metric: "0 recompute",
      metricLabel: "on DRA scale-down events",
    },
    {
      id: 3,
      tag: "Storage",
      headline: "EBS gp3 is the recommended default — NVMe only at >500TB scale",
      detail: "EBS gp3 delivers up to 80K IOPS per volume and survives node failures by reattaching to new pods. For most workloads (up to 500TB), 4×1TB gp3 per worker is sufficient — TPC-DS 10TB peaked at <20% disk utilization across 6 workers. NVMe (i4i, r8gd) adds 500K–1M+ IOPS but requires node affinity and mandatory replication since data doesn't survive node loss.",
      metric: "80K IOPS",
      metricLabel: "EBS gp3 per volume (default)",
    },
    {
      id: 4,
      tag: "Critical Config",
      headline: "Set localShuffleReader=false or every job will throw FileNotFoundException",
      detail: "spark.sql.adaptive.localShuffleReader.enabled defaults to true in Spark 3.x. When Celeborn is the shuffle manager, this setting bypasses Celeborn entirely and tries to read shuffle files locally — which don't exist — causing FileNotFoundException on every fetch. This single misconfiguration is the most common reason Celeborn jobs fail. Set it to false in every Spark job config.",
      metric: "Job killer",
      metricLabel: "most common Celeborn misconfiguration",
    },
    {
      id: 5,
      tag: "Graceful Shutdown",
      headline: "Fixed ports + 720s termination grace period enable safe rolling restarts",
      detail: "Dynamic ports (port=0) cause AssertionError on every graceful shutdown. Pin celeborn.worker.rpc/fetch/push/replicate.port to fixed values (9091–9094). Set terminationGracePeriodSeconds=720 (600s graceful shutdown + 120s buffer). Configure celeborn.worker.graceful.shutdown.timeout=600s. With replication enabled, rolling restarts complete in ~70s per worker with 20–30 retried FileNotFound errors — all handled automatically.",
      metric: "720s",
      metricLabel: "termination grace (600s + 120s buffer)",
    },
    {
      id: 6,
      tag: "AZ Topology",
      headline: "Pin Celeborn workers to the same AZ as Spark executors — cross-AZ costs $0.01/GB",
      detail: "Spread Masters across 3 AZs for control-plane resilience, but keep Workers in a single AZ co-located with Spark executors. Every shuffle byte that crosses an AZ boundary costs $0.01/GB — on a 10TB shuffle job that's $100 in transfer fees per run. If an AZ fails, provision new workers in a surviving AZ within 2–3 minutes rather than routing across zones.",
      metric: "$0.01/GB",
      metricLabel: "cross-AZ transfer cost to avoid",
    },
    {
      id: 7,
      tag: "Karpenter",
      headline: "Set consolidationPolicy=WhenEmpty and add a PodDisruptionBudget",
      detail: "Configure Karpenter with consolidationPolicy: WhenEmpty for Celeborn worker nodes — this prevents Karpenter from evicting workers due to underutilization or drift while shuffle data is active. Pair this with a PodDisruptionBudget (maxUnavailable: 1) to ensure only one worker pod is disrupted at a time. Manual decommission via the REST API (POST /api/v1/workers/{id}/decommission) is the safe path for intentional rotation.",
      metric: "maxUnavailable: 1",
      metricLabel: "PDB for safe worker rotation",
    },
    {
      id: 8,
      tag: "Replication",
      headline: "Enable push replication — it's mandatory for NVMe and free insurance for EBS",
      detail: "Set spark.celeborn.client.push.replicate.enabled=true. For NVMe workers, replication is mandatory: node failure means permanent shuffle loss without it. For EBS, replication turns a ~70s worker restart into a transparent event with automatic retries (maxRetriesForEachReplica=5, retryWait=15s). The overhead is one extra network write per shuffle block — negligible compared to the cost of recomputation.",
      metric: "Zero loss",
      metricLabel: "on worker restart with replication",
    },
  ],
};
