import type { Framework } from "../types";

export const trino: Framework = {
  id: "trino",
  name: "Trino",
  short: "Trino",
  category: "Query Engines",
  color: "#C751EE",
  tagline: "Federated SQL across any data source",
  signals: [
    {
      id: 1,
      tag: "Memory",
      headline: "query.max-memory is cluster-wide, max-memory-per-node is per node — both matter",
      detail: "query.max-memory caps total memory across all workers for a single query. query.max-memory-per-node caps per-worker allocation. Misconfiguring one while leaving the other high causes OOM cascades. For ad-hoc analytics on EKS: max-memory=100GB, max-memory-per-node=8GB. Test with your p95 query before going to production.",
      metric: "2× safer",
      metricLabel: "memory allocation",
    },
    {
      id: 2,
      tag: "Caching",
      headline: "Trino fs.cache on local NVMe makes repeated S3 scans nearly free",
      detail: "Enable native Trino file caching: fs.cache.enabled=true, fs.cache.directories=/tmp/cache/. Use NVMe instance store (i3en, m6id) as the cache directory. Workers cache S3 files locally. BI dashboards refreshing every 5 min see 90% fewer S3 GET requests and 60% lower data transfer costs.",
      metric: "−90%",
      metricLabel: "repeat S3 scan requests",
    },
    {
      id: 3,
      tag: "Kubernetes",
      headline: "Coordinator on On-Demand, workers on Spot with graceful shutdown",
      detail: "Trino on EKS supports graceful pod shutdown — workers drain in-flight query fragments before terminating. Configure terminationGracePeriodSeconds appropriately. Coordinator must run On-Demand — it is the single point of query orchestration. Losing the coordinator kills all in-flight queries.",
      metric: "0 query failures",
      metricLabel: "on Spot reclamation",
    },
    {
      id: 4,
      tag: "Autoscaling",
      headline: "KEDA + Karpenter replaces all manual Trino scaling operations",
      detail: "KEDA scales Trino worker pods based on QueuedQueries Prometheus metric. Karpenter provisions nodes when pods are pending. Target 85% CPU for Graviton instances (they saturate more linearly than x86). Together they eliminate over-provisioning — workers scale to zero between batch windows, saving real money.",
      metric: "0 manual ops",
      metricLabel: "to scale Trino",
    },
    {
      id: 5,
      tag: "Multi-cluster",
      headline: "Separate Trino clusters for ETL, Interactive, and BI — never share",
      detail: "A single Trino cluster shared between ETL jobs, interactive analysts, and BI dashboards creates resource starvation. Three Helm releases, three Karpenter NodePools: ETL on Spot memory-optimized instances; Interactive on On-Demand; BI on Graviton r6g/r7g. No query class starves another, SLAs hold.",
      metric: "3 clusters",
      metricLabel: "zero cross-class starvation",
    },
    {
      id: 6,
      tag: "HA",
      headline: "Two coordinator replicas with PodAntiAffinity — Trino HA isn't optional",
      detail: "A single Trino coordinator is a SPOF. Set replicas: 2, PodAntiAffinity to force them onto separate nodes, PodDisruptionBudget minAvailable: 1. Coordinator config: 40Gi memory, 4–6 CPU cores. This prevents routine EKS node maintenance from wiping out your entire query engine.",
      metric: "99.9%",
      metricLabel: "coordinator availability",
    },
    {
      id: 7,
      tag: "Graviton",
      headline: "Graviton3 for Trino workers: 20% lower cost, better throughput",
      detail: "Graviton3 (r6g, r7g, r8g) delivers better performance-per-dollar for Trino than equivalent x86 instances for analytical workloads. r6g.4xlarge is the sweet spot for memory-intensive queries. KEDA target CPU at 85% — Graviton saturates more linearly. Mix with x86 in Karpenter NodePool for Spot flexibility.",
      metric: "20% lower",
      metricLabel: "cost vs x86 equivalent",
    },
    {
      id: 8,
      tag: "Exchange Manager",
      headline: "S3 Exchange Manager makes Trino Fault Tolerant on Spot clusters",
      detail: "Configure Trino's Exchange Manager with S3 as the backend (fault-tolerant execution mode). When a worker pod crashes mid-query, FTE retries only the failed fragment — not the whole query. Config: exchange.base-uri=s3://exchange-bucket, 64MB upload part size. Essential for Spot-heavy worker clusters.",
      metric: "FTE on",
      metricLabel: "query survives worker loss",
    },
    {
      id: 9,
      tag: "Iceberg",
      headline: "Iceberg hidden partitioning eliminates partition mismatches at query time",
      detail: "Traditional Hive partitioning forces analysts to include partition columns in WHERE clauses. Iceberg hidden partitioning handles this transparently — you query by value, Iceberg figures out which partitions to read. Combined with Trino's cost-based optimizer and dynamic filtering, scan volume drops dramatically.",
      metric: "No partition",
      metricLabel: "columns in WHERE clauses",
    },
  ],
};
