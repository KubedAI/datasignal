import type { Framework } from "../types";

export const flink: Framework = {
  id: "flink",
  name: "Apache Flink",
  short: "Flink",
  category: "Stream Processing",
  color: "#00C9FF",
  tagline: "Stateful stream processing at millisecond latency",
  signals: [
    {
      id: 1,
      tag: "State Backend",
      headline: "Use RocksDB for operator state above 1GB — heap won't survive",
      detail: "The heap state backend is fast but hard-limited by JVM heap size. RocksDB spills to disk and handles 100× the JVM heap in state. On EKS, mount gp3 EBS volumes at /tmp/flink-rocksdb with IOPS 3000+. For state > 50GB per operator, NVMe instance store (m6id) is the right choice.",
      metric: "100×",
      metricLabel: "more state vs heap backend",
    },
    {
      id: 2,
      tag: "Checkpointing",
      headline: "Incremental S3 checkpoints: 50GB state checkpoint in 3s not 45s",
      detail: "Full checkpoints upload the entire RocksDB state to S3 — stalling pipelines for minutes at high state sizes. Incremental checkpointing uploads only changed SST files. A 50GB state checkpoint drops from 45s full to under 3s incremental. Enable: state.backend.incremental=true.",
      metric: "45s → 3s",
      metricLabel: "checkpoint time at 50GB state",
    },
    {
      id: 3,
      tag: "Kubernetes",
      headline: "Native K8s mode: TaskManagers appear on-demand and vanish after use",
      detail: "Flink Native Kubernetes mode lets the JobManager request TaskManagers directly from the Kubernetes API — no fixed cluster. TaskManagers scale up per-job and terminate when idle. Pair with Karpenter: nodes provision in <60s as TaskManagers are requested, terminate when the job ends.",
      metric: "0 idle",
      metricLabel: "TaskManagers between jobs",
    },
    {
      id: 4,
      tag: "Backpressure",
      headline: "Credit-based flow control: never disable it, ever",
      detail: "Flink's network stack uses credit-based flow control. When a downstream operator is slow, upstream buffers data in off-heap managed memory — not the JVM heap. Disabling this fills the heap and triggers OOM. Nearly every OOM issue in Flink streaming traces back to flow control being disabled or misconfigured.",
      metric: "0 OOM",
      metricLabel: "with credit-flow enabled",
      light: "red",
    },
    {
      id: 5,
      tag: "Application Mode",
      headline: "Flink Application Mode: one job per cluster, full resource isolation",
      detail: "Application Mode runs each Flink job with its own JobManager — no multi-tenancy resource contention. The JobManager is inside the cluster, not on the client. TaskManagers terminate when the job finishes. With Karpenter, the entire cluster provisions and deprovisions automatically per job run.",
      metric: "0 cross-job",
      metricLabel: "resource contention",
    },
    {
      id: 6,
      tag: "Kafka",
      headline: "Set Flink Kafka consumer parallelism = Kafka partition count",
      detail: "A Flink Kafka source reads one partition per subtask. If parallelism < partitions, subtasks consume multiple partitions and backpressure builds. If parallelism > partitions, extra subtasks are idle waste. Set the source operator parallelism exactly to the number of Kafka topic partitions.",
      metric: "1:1",
      metricLabel: "parallelism to partitions",
    },
    {
      id: 7,
      tag: "Memory",
      headline: "taskmanager.memory.managed.fraction controls RocksDB write buffer",
      detail: "Flink's managed memory (off-heap) is where RocksDB gets its write buffer and block cache. Default fraction is 0.4. For state-heavy jobs, increase to 0.6 and reduce heap accordingly. Getting this wrong causes RocksDB to spill to disk excessively or the JVM to OOM — both quietly destroy throughput.",
      metric: "0.6",
      metricLabel: "optimal managed.fraction for state jobs",
    },
    {
      id: 8,
      tag: "EMR",
      headline: "EMR on EKS for Flink removes checkpoint management overhead",
      detail: "Amazon EMR on EKS manages Flink checkpoints to S3 automatically — no manual RocksDB configuration, no checkpoint cleanup crons. The EMR Flink runtime patches are tested and production-hardened by AWS. Use for pipelines where operational burden matters more than bleeding-edge Flink version.",
      metric: "0 ops",
      metricLabel: "checkpoint management tasks",
    },
  ],
};
