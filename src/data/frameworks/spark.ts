import type { Framework } from "../types";

export const spark: Framework = {
  id: "spark",
  name: "Apache Spark",
  short: "Spark",
  category: "Compute",
  color: "#E25A1C",
  tagline: "Distributed compute at petabyte scale",
  signals: [
    {
      id: 1,
      tag: "Storage",
      headline: "NVMe Instance Store beats EBS for shuffle — always",
      detail: "Local NVMe SSD (r6id, m6id, i3en) delivers 10–20x faster shuffle than gp3 EBS. Zero network hops means shuffle-heavy TPC-DS queries finish 40–60% faster. Karpenter provisions NVMe nodes on-demand — no pre-warming, no pre-provisioned node groups needed.",
      metric: "10–20×",
      metricLabel: "vs EBS shuffle",
      light: "green",
    },
    {
      id: 2,
      tag: "Scheduling",
      headline: "Gang Scheduling with YuniKorn prevents zombie Spark drivers",
      detail: "Without gang scheduling, the Spark driver starts and consumes resources while executors queue pending — a zombie job burning money. YuniKorn's all-or-nothing placement schedules driver + all executors atomically. No partial allocation, no deadlock, no wasted On-Demand driver node.",
      metric: "0 zombie jobs",
      metricLabel: "with gang scheduling",
    },
    {
      id: 3,
      tag: "Memory",
      headline: "Push spark.memory.fraction to 0.8 on large r6i nodes",
      detail: "On r6i.8xlarge+ (>200GB RAM), Kubernetes overhead is <5% of total memory. Push spark.memory.fraction to 0.8 and spark.memory.storageFraction to 0.3. You gain ~15% more executor memory at zero cost. The default 0.6 was designed for JVM-heavy older deployments.",
      metric: "+15%",
      metricLabel: "executor memory, free",
    },
    {
      id: 4,
      tag: "Cost",
      headline: "Spot Instances for executors — driver always On-Demand",
      detail: "Spark with checkpointing tolerates Spot interruptions gracefully. Run executors on Spot (c6i, m6i Spot pools) and keep only the driver on On-Demand via a separate Karpenter NodePool. Karpenter handles Spot rebalancing automatically. Typical savings: 60–70% on compute.",
      metric: "60–70%",
      metricLabel: "compute cost savings",
      light: "yellow",
    },
    {
      id: 5,
      tag: "Multi-Tenancy",
      headline: "One YuniKorn queue per team — not per namespace",
      detail: "Teams create multiple namespaces (sandbox, test, prod) but share one YuniKorn queue parent. Placement rules map all team namespaces to their queue automatically. Quota enforced at team level — not per-namespace. Simpler config, consistent cost attribution, no quota fragmentation.",
      metric: "30 teams",
      metricLabel: "one scheduler config",
    },
    {
      id: 6,
      tag: "Celeborn",
      headline: "Celeborn external shuffle removes the 1TB local disk bottleneck",
      detail: "Beyond 1000 concurrent Spark tasks or 500GB–1TB shuffle data per job, local shuffle disk is the bottleneck. Apache Celeborn decouples shuffle from compute: dedicated shuffle nodes (StatefulSets) shared across all teams. Reduces executor disk I/O by 80%, enables true Dynamic Resource Allocation.",
      metric: "80%",
      metricLabel: "less disk I/O per executor",
    },
    {
      id: 7,
      tag: "Karpenter",
      headline: "Separate NodePools for driver and executor — protect the driver at all costs",
      detail: "Driver NodePool: On-Demand only, consolidation disabled. Executor NodePool: Spot-first with graceful decommissioning. Terminating the Spark driver kills the entire job — losing hours of compute. A Spot interruption on an executor is recoverable; a Spot interruption on the driver is not.",
      metric: "0 driver deaths",
      metricLabel: "from Spot reclamation",
    },
    {
      id: 8,
      tag: "Decommission",
      headline: "spark.executor.decommission.enabled=true saves hours of recompute",
      detail: "When Karpenter consolidates a node or Spot reclaims an instance, Spark can migrate shuffle blocks to surviving executors before the node dies. Set spark.executor.decommission.enabled=true and spark.storage.decommission.shuffleBlocks.enabled=true. Executors drain gracefully rather than dying mid-task.",
      metric: "Zero recompute",
      metricLabel: "on Spot interruption",
    },
    {
      id: 9,
      tag: "Scheduling",
      headline: "\"Most allocated\" bin-packing beats Kubernetes default for Spark",
      detail: "Kubernetes default scheduler uses 'least allocated' — spreading pods evenly. For Spark this wastes nodes. 'Most allocated' (bin-packing) fills nodes before opening new ones. YuniKorn and Volcano both implement bin-packing natively. Result: 30% fewer nodes, proportionally lower cluster cost.",
      metric: "−30%",
      metricLabel: "nodes needed for same job",
      light: "green",
    },
    {
      id: 10,
      tag: "Storage",
      headline: "Reuse PVCs across executor restarts with one config flag",
      detail: "spark.kubernetes.executor.reusePersistentVolume=true — when a Spot executor is evicted and a replacement starts, it reattaches the same PVC with intact shuffle data. Combine with Karpenter node affinity to keep the volume accessible. Eliminates the need to recompute shuffle stages after eviction.",
      metric: "Zero data loss",
      metricLabel: "on executor eviction",
    },
    {
      id: 11,
      tag: "Networking",
      headline: "Pin Spark pods to a single AZ and eliminate inter-AZ shuffle costs",
      detail: "Cross-AZ shuffle traffic is charged as inter-AZ data transfer (up to $0.02/GB). For shuffle-heavy TPC-DS workloads, cross-AZ traffic can be 60–80% of total network. Use topology-aware Karpenter NodePools with az-specific requirements to colocate driver and all executors in one AZ.",
      metric: "$0",
      metricLabel: "inter-AZ shuffle cost",
    },
    {
      id: 12,
      tag: "DNS",
      headline: "Scale CoreDNS before Spark DNS throttling generates unknownHostExceptions",
      detail: "Spark executors create new connections constantly — each triggers a DNS lookup. At >1024 packets/second per CoreDNS replica, requests are throttled → unknownHostException cascades. Scale CoreDNS pods horizontally as executor counts grow. Monitor with Prometheus coredns_dns_requests_total.",
      metric: "1024 pkt/s",
      metricLabel: "is the CoreDNS throttle limit",
    },
    {
      id: 13,
      tag: "GPU",
      headline: "NVIDIA RAPIDS on EKS: 7x faster ETL with zero Spark code changes",
      detail: "GPU-accelerated Spark with NVIDIA RAPIDS (cuDF plugin) delivers up to 7x ETL speedup without modifying SQL queries. Works via a transparent Spark plugin. Best for wide transformations: joins, aggregations, sort operations on large Parquet datasets. EMR on EKS supports RAPIDS out of the box.",
      metric: "7×",
      metricLabel: "faster ETL vs CPU Spark",
    },
  ],
};
